{
  "routes": [
    {
      "method": "GET",
      "path": "/networks",
      "handler": "networks.find",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/networks/count",
      "handler": "networks.count",
      "config": {
        "policies": []
      }
    },
    {
      "method": "GET",
      "path": "/networks/:id",
      "handler": "networks.findOne",
      "config": {
        "policies": []
      }
    },
    {
      "method": "POST",
      "path": "/networks",
      "handler": "networks.create",
      "config": {
        "policies": []
      }
    },
    {
      "method": "PUT",
      "path": "/networks/:id",
      "handler": "networks.update",
      "config": {
        "policies": []
      }
    },
    {
      "method": "DELETE",
      "path": "/networks/:id",
      "handler": "networks.delete",
      "config": {
        "policies": []
      }
    }
  ]
}
